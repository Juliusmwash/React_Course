Css with React
Styled the Chatbot Project
Flexbox = create a flexible layout
Ternary Operator (?:) = if-else statement directly in the JSX
hooks = insert React features into a component
useEffect = run code after component is created or updated
useRef = save an HTML element from the component
Created the auto scroll feature
State Management = learned how to use useState to track input and responses
Props = passed data between components for better reuse and composition
Handling Events = captured user input and button clicks to drive chat flow
Conditional Rendering = showed different UI based on chat state and loading status